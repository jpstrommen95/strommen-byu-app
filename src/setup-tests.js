// Sets up testing environment to support `test:ci` command through custom `jest.config.js` config
/* eslint-disable-next-line import/no-extraneous-dependencies */ // setup-tests isn't in build
require('@testing-library/jest-dom');
