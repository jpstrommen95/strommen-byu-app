// disabling because eslint config spec does not permit top-level default prop
// eslint-disable-next-line import/no-import-module-exports
import configService from './config.service';

module.exports = configService;
